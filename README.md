# Termux Config

Personal Termux configuration.

### Setup
Clone the repository to your home folder and run the setup script:
```bash
git clone https://github.com/jehan593/termux-config.git ~/termux-config
cd ~/termux-config
bash setup.sh
```

### Reset
To revert all changes, run the reset script:
```bash
bash reset.sh
```
